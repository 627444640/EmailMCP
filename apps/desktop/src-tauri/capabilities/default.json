{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions for the Email MCP desktop app.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:default"]
}
